export interface WorkCardProps {
  category: string;
  title: string;
  description?: string;
  gradient?: boolean;
  className?: string;
}

export interface WorkVideoCardProps {
  videoUrl: string;
  className?: string;
}